import {
	Visibility,
	VisibilityOff,
	Apps
} from '@mui/icons-material';

export const helpIcon = () => {

	const iconObject = {
		visibility: Visibility,
		visibilityOff: VisibilityOff,
		default: Apps
	}

	return {
		iconObject
	}

}
